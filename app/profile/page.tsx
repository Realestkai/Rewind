import Link from "next/link"
import { cookies } from "next/headers"
import { readSession } from "@/lib/auth"
export default async function ProfilePage(){ const user = readSession((await cookies()).get("ryvn_discord_user")?.value); return <main className="account-page"><header className="site-header"><Link href="/" className="wordmark">RYVN<span>®</span></Link><Link className="quiet-link" href="/settings">Settings</Link></header><section>{user ? <><span className="account-avatar">{user.username.slice(0,1).toUpperCase()}</span><p className="eyebrow">DISCORD CONNECTED</p><h1>{user.username}</h1><p>Your RYVN profile is connected. Purchases, reviews, commission updates, and private support tickets will appear here.</p><Link className="button" href="/commissions">Start a commission</Link></> : <><span className="account-avatar">R</span><p className="eyebrow">YOUR PROFILE</p><h1>Connect Discord to begin.</h1><p>Purchases, reviews, commission updates and tickets will appear here after your account is linked.</p><Link className="button" href="/login">Sign in with Discord</Link></>}</section></main>}
